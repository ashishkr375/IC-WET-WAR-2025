/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'www.nitp.ac.in',
      'ugcounselor-content.s3.ap-south-1.amazonaws.com'
    ],
  },
};

export default nextConfig;
